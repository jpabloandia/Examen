import 'package:flutter/material.dart';

class VerProductScreen extends StatelessWidget {
  const VerProductScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('VerProductScreen'),
      ),
      body: const Center(
        child: Text('VerProductScreen'),
      ),
    );
  }
}
